# xplatform-memory

memory hack template for linux and windows

## build all binaries

`cargo build --release`

### building specific binary

`cargo build --release --bin xplatform-patch-twfc`

### running specific binary with cargo

`cargo run --release --bin xplatform-patch-twfc`
